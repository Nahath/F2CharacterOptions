[Perks]
Enable=1

; Heave Ho! (perk ID 35).
; Positive STR = minimum requirement; negative STR = maximum requirement.
; STR=0 sets a minimum of 0, which removes the vanilla STR<=8 cap.
; STR=-99999 would leave vanilla unchanged (sentinel = ignore field).
